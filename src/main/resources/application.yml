server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shopust?useSSL=false
    username: root
    password: pass
    driver-class-name: com.mysql.cj.jdbc.Driver